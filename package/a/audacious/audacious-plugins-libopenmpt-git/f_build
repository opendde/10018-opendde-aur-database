build() {
    cd "${pkgname%-libopenmpt-git}"
    if [ "$_use_meson" = 1 ]; then
        arch-meson build
        meson compile -C build
    else
        autoreconf
        ./configure --prefix=/usr
        make
    fi
}
