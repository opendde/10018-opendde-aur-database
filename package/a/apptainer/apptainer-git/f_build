build() {
  cd "apptainer"

  # provide version to build script
  echo "$pkgver" > VERSION

  ./mconfig \
    --prefix="/usr" \
    --sysconfdir="/etc" \
    --libexecdir="/usr/lib" \
    --localstatedir="/var/lib" \
    -P release-stripped
  make -C "builddir"
}
