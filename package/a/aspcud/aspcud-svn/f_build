build() {
    cd "$srcdir/$pkgname"
    mkdir -p build/release
    cd build/release
    cmake \
	-DCMAKE_INSTALL_PREFIX:PATH=/usr \
	-DCUDF2LP_LOC=cudf2lp \
    	-DGRINGO_LOC=gringo \
	-DCLASP_LOC=clasp \
	-DCMAKE_BUILD_TYPE=Release ../..
    make
}
