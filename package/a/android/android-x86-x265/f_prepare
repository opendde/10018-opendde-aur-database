prepare() {
    cd "${srcdir}"/x265_${pkgver}

    case "$_android_arch" in
        aarch64)
            patch -Np1 -i "../0001-Disable-neon.patch"
            ;;
        *)
            ;;
    esac
}
