build() {
    cd "$srcdir"/bzip2-$pkgver
    source android-env ${_android_arch}

    make libbz2.a \
        CC=${ANDROID_CC} \
        AR=${ANDROID_AR} \
        RANLIB=${ANDROID_RANLIB} \
        PREFIX=${ANDROID_PREFIX} \
        $MAKEFLAGS
}
