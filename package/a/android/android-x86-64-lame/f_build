build() {
    cd "${srcdir}"/lame-${pkgver}
    source android-env ${_android_arch}

    android-${_android_arch}-configure \
        --disable-frontend \
        --enable-nasm

    make $MAKEFLAGS
}
