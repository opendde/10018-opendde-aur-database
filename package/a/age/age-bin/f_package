package() {
    cd "${srcdir}/age"

    # Install age
    install -D -m0755 age \
        "${pkgdir}/usr/bin/age"

    # Install age-keygen
    install -D -m0755 age-keygen \
        "${pkgdir}/usr/bin/age-keygen"
}
