build() {
  cd "${srcdir}/${_pkgname}"
  ./autogen.sh
  ./configure \
    --prefix=/usr \
    --with-alsa \
    --with-dssi \
    --with-jack \
    --with-lv2 \
    --with-nsm \
    --with-pandoc \
    --with-vst
  make
}
