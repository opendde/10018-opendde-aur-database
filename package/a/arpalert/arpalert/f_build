build() {
  cd "${srcdir}/${pkgname}-${pkgver}"
  ./configure --prefix=/ --exec-prefix=/usr \
              --includedir=/usr/include \
              --datarootdir=/usr/share \
              --sbindir=/usr/bin
  make
}
