build() {
  cd $_srcdir

  source $_target-environment

  mkdir -p build-$_target && pushd build-$_target
  make \
    CC=$_target-gcc \
    CXX=$_target-g++ \
    AR=$_target-ar \
    STRIP=$_target-strip \
    -C ..
  popd
}
