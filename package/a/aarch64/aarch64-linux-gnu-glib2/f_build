build() {
  cd $_srcdir

  mkdir -p build-$_target && pushd build-$_target
  CFLAGS+=" -DG_DISABLE_CAST_CHECKS"

  $_target-meson \
    -D selinux=disabled \
    -D man=true \
    ..

  ninja
  popd
}
