package() {
  cd "agate"

  cargo install \
    --no-track \
    --locked \
    --root "$pkgdir/usr" \
    --path "$srcdir/agate"
  install -Dm644 "LICENSE-MIT" -t "$pkgdir/usr/share/licenses/agate"
}
