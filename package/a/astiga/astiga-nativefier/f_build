build() {
  cd "${srcdir}"
  
  nativefier \
    --name "Astiga" \
    --icon "${pkgname}.png" \
    --single-instance \
    --tray \
    --maximize \
    --disable-dev-tools \
    "https://play.asti.ga/overview"
}
