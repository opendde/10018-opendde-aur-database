build() {
  cd $pkgname
  # upstream bug: Configure ignores --prefix option
  ./configure \
	--prefix=/usr \
	--sysconfdir=/etc \
	--enable-pam \
	--enable-hash \
	--enable-xrender \
	--enable-imlib2 \
	--with-dunst \
	--with-xbacklight
  make V=0
}
