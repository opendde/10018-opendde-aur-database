build() {
  cd "$pkgname-$pkgver"
  autoreconf -fi
  CFLAGS+=" -fcommon" ./configure \
	 --prefix=/usr \
	 --disable-ffmpeg \
	 --disable-gconf \
	 --disable-gnome-vfs \
	 --disable-gtk-doc \
	 --disable-doc
  make --jobs=1
}
