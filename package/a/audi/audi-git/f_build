build() {
  mkdir -p build

  cd $_gitname

  git submodule init
  git submodule update

  cd ../build

  cmake ../$_gitname \
    -DCMAKE_INSTALL_PREFIX:PATH=/usr \
    -DBUILD_PYAUDI=ON \
    -DBUILD_TESTS=OFF || return 1

  make $MAKEFLAGS || return 1
}
