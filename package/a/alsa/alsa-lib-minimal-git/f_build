build() {
  cd alsa-lib
  ./configure --prefix=/usr \
              --without-debug \
	      --disable-debug \
	      --enable-symbolic-functions \
	      --disable-rawmidi \
	      --disable-seq

  make
}
