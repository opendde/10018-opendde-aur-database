package() {
  cd "$pkgname"
  install -D -m 755 adb-record.sh "$pkgdir/usr/bin/adb-record"
  install -D -m 755 adb-screen-capture.sh "$pkgdir/usr/bin/adb-screen-capture"
  install -Dm644 LICENSE "$pkgdir/usr/share/licenses/adb-screen-tools/LICENSE"
}
