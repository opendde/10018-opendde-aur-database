build() {
    rm -rf "${srcdir}/build"
    mkdir "${srcdir}/build"
    cd "${srcdir}/build"
    cmake -G 'Unix Makefiles' \
        -DCMAKE_INSTALL_PREFIX=/usr \
        -DBLOATY_ENABLE_CMAKETARGETS=OFF \
        "${srcdir}/${pkgname}"
    make
}
