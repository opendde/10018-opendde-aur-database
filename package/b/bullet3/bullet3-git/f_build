build() {
  cd "$srcdir"

  cmake bullet3 \
  -DCMAKE_INSTALL_PREFIX=/usr \
  -DBUILD_SHARED_LIBS=ON \
  -DBUILD_BULLET3=ON \
  -DBUILD_BULLET2_DEMOS=ON \
  -DBUILD_CPU_DEMOS=ON \
  -DBUILD_UNIT_TESTS=OFF \
  -DINSTALL_LIBS=ON

  make
}
