build() {
  cd $srcdir/$pkgname
  ./configure --exec_prefix=/usr      \
              --prefix=/usr           \
              --mandir=/usr/share/man \
              --enable-ipv6           \
              --with-plugins          \
              --with-ssl
  make 
}
