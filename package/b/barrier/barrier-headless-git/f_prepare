prepare() {
    cd $pkgname
  
    git submodule update --init --recursive

    for patch in "${srcdir?}"/*.patch; do
        if [ -f "${patch?}" ]; then
            patch -Np1 -i "${patch?}"
        fi
    done
}
